package com.dy.minichat;

import org.junit.jupiter.api.Test;

// @SpringBootTest
public class MemberServiceTest {
    // @Autowired
    // private AppService appService;

    @Test
    public void test(){
       // AppService appService = new AppService(// //);
        /*
        PaymentRepository paymentRepository = new PaymentRepository();
        // 결제에 대한 셋팅
        //
         //
         //
         //
         //
         //
        PaymentRepository mockPaymentRepository = mockito.mock(PaymentRepository);
        mockPaymentRepository.pay().thenReturn(new FakePaymentObject());


        AppService appService = new AppService(paymentRepository);

        // test
        appService.pay();

        // assert
        // 1. appService.pay(); 의 리턴값이 올바른가?
        // 2. 가짜로 만들어준 객체가 의도한대로 잘 호출뙜나?
        assert.assertEquals(1, mockPaymentRepository.pay().callCount());
        */

    }
}
